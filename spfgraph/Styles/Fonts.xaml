<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <FontFamily
        x:Key="LatoRegular">pack://appliction;,,,/Fonts/#Lato Regular</FontFamily>

    <Style
        TargetType="{x:Type Control}"
        x:Key="BaseControl">
        <Setter
            Property="FontFamily"
            Value="{StaticResource LatoRegular}" />
        <Setter
            Property="FontSize"
            Value="14" />
    </Style>

    <Style
        TargetType="{x:Type Button}"
        BasedOn="{StaticResource BaseControl}" />
    <Style
        TargetType="{x:Type MenuItem}"
        BasedOn="{StaticResource BaseControl}" />
    <Style
        TargetType="{x:Type Label}"
        BasedOn="{StaticResource BaseControl}" />

    <Style
        TargetType="{x:Type TextBlock}">
        <Setter
            Property="FontSize"
            Value="14" />
        <Setter
            Property="FontFamily"
            Value="{StaticResource LatoRegular}" />
    </Style>

</ResourceDictionary>