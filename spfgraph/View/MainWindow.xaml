<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        x:Name="AppWindow"
        WindowStyle="None"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        Title="spfgraph" Height="540" Width="960">
    <Window.Resources>
        <Style TargetType="{x:Type local:MainWindow}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Window}">
                        
                        <!--Outer border for shadow-->
                        <Border Padding="{Binding OuterMarginThinckness, FallbackValue=10}" x:Name="Container">
                            
                            <!--Main window outline--> 
                            <Grid>
                                <Border CornerRadius="{Binding WindowCornerRadius, FallbackValue=5}" Background="{Binding WhiteColorBrush, FallbackValue=White}">

                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="10" Opacity="0.5" />
                                    </Border.Effect>
                                </Border>
                                
                                <!--The main window content-->

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="{Binding TitleHeightGridLength, FallbackValue=22}"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <!--Top bar-->
                                    <Grid Background="#666" Grid.Row="0" Panel.ZIndex="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Viewbox HorizontalAlignment="Center" Grid.Column="1">
                                            <TextBlock Text="1"/>
                                        </Viewbox>

                                    </Grid>
                                </Grid>
                                
                                
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome 
            ResizeBorderThickness="{Binding ResizeBorderThickness}"
            CaptionHeight="{Binding TitleHeight}"
            GlassFrameThickness="0"
            CornerRadius="0"
            />
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="22"/>
            <RowDefinition Height="35"/>
            <RowDefinition />
            <RowDefinition Height="22"/>
        </Grid.RowDefinitions>

       

        <!--Top dock panel-->
        <DockPanel Grid.Row="1" Style="{StaticResource ResourceKey=topBar}">
            <StackPanel Style="{StaticResource ResourceKey=stackPanelForBar}">
                <Menu Style="{StaticResource ResourceKey=menuForBar}">
                    <MenuItem Width="55" Style="{StaticResource ResourceKey=menuItemForBar}">
                        <MenuItem.Header>
                            <TextBlock Width="25" Text="File" HorizontalAlignment="Center"  Margin="7 0 0 0"/>
                        </MenuItem.Header>
                        <MenuItem Header="Save"/>
                        <MenuItem Header="Open" />
                        <MenuItem Header="New" />
                    </MenuItem>
                    <MenuItem Style="{StaticResource ResourceKey=menuItemForBar}" Header="Properties">
                        <MenuItem Header="Item 1" />
                        <MenuItem Header="Item 2" />
                        <MenuItem Header="Item 3" />
                    </MenuItem>
                </Menu>
            </StackPanel>
        </DockPanel>

        <!--Main page-->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <!--Left side of the main page-->
            <Border Background="#ADD7F6" Padding="15 10 15 10">
                <StackPanel >
                    <TextBlock Text="Path" Margin="0 0 0 5"/>
                    <TextBox Grid.Column="0" IsReadOnly="False" Text="" Margin="0 0 0 10"/>
                    <Button Grid.Column="1" Content="Change Path" Width="120" Height="30" Margin="0 0 0 10" />
                    <Button Content="Build Graph" Width="120" Height="30" Margin="0 0 0 10"/>
                </StackPanel>
            </Border>

            <!--Right side of the main page-->
            <Border Grid.Column="1" Background="White">
                <StackPanel >
                    <TextBlock FontSize="72" Text="{Binding Test}" />

                </StackPanel>
                <!--<Canvas x:Name="canvas">
                
                </Canvas>-->
            </Border>
        </Grid>

        <!--Bottom dock panel-->
        <DockPanel Height="22" Grid.Row="3" Background="{StaticResource CleanBlueBrush}" >
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock VerticalAlignment="Center" Background="{StaticResource CleanBlueBrush}" Foreground="White" Text="Status" Margin="15 0 0 0"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
